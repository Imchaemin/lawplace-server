generator client {
  provider = "prisma-client-js"
  output   = "./clients/client-test"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_TEST")
}

model User {
  id String @id @default(uuid())
}
